{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            <div class="mb-3">
                <label for="description" class="form-label">Your Pincode</label>
                <label for="description" class="form-label">*Must contain 4 numbers</label>
                {{ form_row(form.pincode,{
                    id:'pincode',
                }) }}
            </div>
            <div class="mb-3">
                <button type="submit" class=" btn btn-outline-primary btn-lg">Save Changes</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
