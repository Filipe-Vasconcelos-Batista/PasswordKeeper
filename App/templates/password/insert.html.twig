{% extends 'base.html.twig' %}

{% block title %}Insert Passsword{% endblock %}

{% block body %}
    {% if form %}
    <div class="">
        <div class="">
            <div class="">
                <div class="">
                    <div class="">Insert Password</div>
                    <div class="">
                        {% block form %}
                            {{ form_start(form) }}
                            {{ form_end(form) }}
                            <button type="submit">Submit </button>
                        {% endblock %}
                    </div>
                </div>
            </div>
        </div>

    {% else %}
        <div class="" role="alert" id="errorBox">
            <h1>Ups something went wrong?</h1>
        </div>
    {% endif %}
    </div>

{% endblock %}
